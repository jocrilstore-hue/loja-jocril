# Handoff — M0 Scaffold LOJA-ONLINE

**Date:** 2026-04-18
**Branch:** `feature/jocril-port` @ `f58b42c`
**Status:** ✅ Complete. Build passes.

## Goal

Scaffold a Next.js 16 project in `LOJA-ONLINE/` with Jocril design tokens, fonts, and assets wired up — no visual port yet, just foundation.

## Completed

- `package.json` with Next 16.2.3, React 19, TS 5, Tailwind 4.2.2, lucide-react 1.8.0
- `tsconfig.json` (strict, bundler resolution, `@/*` path alias)
- `next.config.ts`, `postcss.config.mjs`, `.gitignore`, `.env.local.example`, `next-env.d.ts`
- `app/layout.tsx` — root layout, `pt-PT`, `data-theme="dark"`, link tags for tokens + responsive CSS
- `app/globals.css` — Tailwind import + `@font-face` for Geist Sans/Mono
- `app/page.tsx` — stub proving fonts + tokens + dark theme render
- `public/fonts/` — geist-sans.woff2, geist-mono.woff2 (copied from `_design_src`)
- `public/styles/colors_and_type.css` — 61 tokens + 7 utility classes
- `public/styles/store-responsive.css` — breakpoints
- `public/assets/` — 9 brand files + 28 portfolio + 8 processes
- `CLAUDE.md` — stack, port philosophy, do-not list
- `git init` + `feature/jocril-port` branch + commit `f58b42c`

## Verified

- `npm install` — 99 packages, 0 vulnerabilities
- `npm run build` — clean. Turbopack. 3 static routes prerendered.
- TypeScript auto-updated tsconfig + next-env.d.ts (intentional, accepted)

## What must NOT regress

- `_design_src/` stays gitignored and untouched — it's the source-of-truth reference.
- CSS tokens are loaded via `<link>` in `app/layout.tsx`, not `@import` in globals.css (public paths don't resolve at CSS build time).
- Dark theme is default via `data-theme="dark"` on `<html>` in layout.
- Portuguese locale: `lang="pt-PT"` on `<html>`.

## Next step → M0b

Scaffold sibling `site-marketing/` at `C:\Users\maria\Desktop\pessoal\jocril\site-marketing\`. Same pattern. Different port (3001).

## Key decisions logged

- Manual scaffold (not `create-next-app`) because the target directory already had `_design_src/`.
- Tokens loaded via `<link>` not CSS `@import` — public paths require runtime loading.
- No shadcn / react-hook-form / zod / framer-motion added — prototype has its own primitive set.
- Used inline styles in stub page to match the port philosophy from day 1.
