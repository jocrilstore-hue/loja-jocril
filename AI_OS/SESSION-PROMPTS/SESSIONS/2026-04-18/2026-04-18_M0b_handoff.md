# Handoff — M0b Scaffold site-marketing

**Date:** 2026-04-18
**Location:** `C:\Users\maria\Desktop\pessoal\jocril\site-marketing\`
**Branch:** `feature/jocril-port` @ `b1d5262`
**Status:** ✅ Complete. Build passes.

## Completed

Same scaffold pattern as LOJA-ONLINE, adjusted for marketing site:
- package.json, tsconfig, next.config, postcss, .gitignore, next-env.d.ts
- `app/layout.tsx` (pt-PT, data-theme="dark", token link), `app/globals.css`, `app/page.tsx` stub
- `public/fonts/` (Geist Sans + Mono) + `public/styles/colors_and_type.css` + `public/assets/` (9 brand + 28 portfolio + 8 processes)
- CLAUDE.md pointing at LOJA-ONLINE's CLAUDE.md for shared conventions
- `npm install` clean (99 packages, 0 vulns)
- `npm run build` clean — 3 static routes prerendered
- `git init` + `feature/jocril-port` + commit `b1d5262`

## Key differences from LOJA-ONLINE

- Dev/start port: **3001** (via package.json scripts) to avoid clash with LOJA-ONLINE on 3000
- Does NOT load `store-responsive.css` (marketing site has its own responsive rules in prototype — will port in M_MKT)
- No `_design_src/` inside — reads from `../LOJA-ONLINE/_design_src/` (single source of truth)

## Next step → M1

Port shared foundation components in `LOJA-ONLINE/components/`. M_MKT will later port the marketing components here in `site-marketing/`.
