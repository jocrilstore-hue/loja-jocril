---
🎯 Model: Sonnet 4.6 (claude-sonnet-4-6) — adaptive thinking, effort: high
Why: deployment config + checklist generation. No novel logic, but mistakes here are production-visible.
---

# Session B8 — Vercel deploy

## Pre-read (mandatory)
1. `CLAUDE.md` in project root
2. `AI_OS/SESSION-PROMPTS/SESSIONS/2026-04-19/2026-04-19_B5-B8_MASTER-PLAN.md`
3. `next.config.js` (current production settings)
4. `.env.local` (identify all vars to replicate in Vercel)

## Pre-conditions (must all be true before starting)
- [ ] B5 committed to `main`
- [ ] B6 committed to `main`
- [ ] B7 committed to `main`
- [ ] `npm run build` passes clean on `main`
- [ ] Maria has a Vercel account and has connected the GitHub repo (or will push and deploy manually)
- [ ] Eupago production credentials in `.env.local` (`EUPAGO_API_KEY`, `EUPAGO_BASE_URL`)
- [ ] Resend API key in `.env.local` (`RESEND_API_KEY`)
- [ ] Clerk production instance created at clerk.com (separate from dev instance)

## Behavioral Rules

### 1. Think Before Doing
- Read `next.config.js` and `.env.local` before touching anything.
- List every env var found in `.env.local` — that's the Vercel dashboard checklist.

### 2. Simplicity First
- No custom server. Vercel handles Next.js natively.
- No Docker, no custom build scripts.
- No CDN config changes — Vercel handles caching.

### 3. No Destructive Git
- Do not force push. Do not rebase. Add commits only.

---

## Step 1 — Audit env vars

Read `.env.local` and list every variable. Produce a checklist in this format:

```
Vercel Environment Variables to set (Settings → Environment Variables):

Required:
  NEXT_PUBLIC_SUPABASE_URL          = [copy from .env.local]
  NEXT_PUBLIC_SUPABASE_ANON_KEY     = [copy from .env.local]
  SUPABASE_SERVICE_ROLE_KEY         = [copy from .env.local] — mark as SENSITIVE
  NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY = [Clerk PRODUCTION key — different from dev]
  CLERK_SECRET_KEY                  = [Clerk PRODUCTION key — SENSITIVE]
  EUPAGO_API_KEY                    = [production key — SENSITIVE]
  EUPAGO_BASE_URL                   = https://clientes.eupago.pt
  NEXT_PUBLIC_SITE_URL              = https://[production-domain]
  RESEND_API_KEY                    = [re_... — SENSITIVE]
  EMAIL_FROM                        = [verified sender address]
  ADMIN_EMAIL                       = [admin notification recipient]
  ADMIN_EMAILS                      = [comma-separated admin emails for Clerk check]

Optional:
  NODE_ENV                          = production  (Vercel sets this automatically)
```

Print this checklist — Maria copies it to Vercel dashboard.

---

## Step 2 — Review `next.config.js`

Check:
- `images.remotePatterns` includes `*.supabase.co` ✓ (already there from prior sessions)
- `images.unoptimized` is conditional on `NODE_ENV === 'development'` ✓
- No localhost or dev-only settings hardcoded

If anything needs fixing, make the minimal change.

---

## Step 3 — Verify `vercel.json` (create if missing)

Check if `vercel.json` exists. If not, create a minimal one:

```json
{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "headers": [
    {
      "source": "/api/webhooks/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
```

Do not add rewrites unless there's a specific need.

---

## Step 4 — Eupago webhook registration

After Vercel deployment is live (Maria does this), the Eupago webhook must be registered.

Produce this instruction for Maria:

```
Post-deploy manual step — Eupago webhook:
1. Log in to clientes.eupago.pt
2. Navigate to API / Webhooks settings
3. Set callback URL to: https://[production-domain]/api/webhooks/eupago
4. Test: GET https://[production-domain]/api/webhooks/eupago → should return {"status":"ok"}
```

---

## Step 5 — Clerk production setup checklist

Produce this instruction for Maria:

```
Post-deploy manual step — Clerk production:
1. clerk.com → Create production instance (separate from dev)
2. Set allowed origins: https://[production-domain]
3. Set redirect URLs: https://[production-domain]/sign-in, /sign-up, /conta
4. Copy NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY + CLERK_SECRET_KEY to Vercel env vars
5. For admin users: in Clerk dashboard → Users → [user] → publicMetadata → set { "role": "admin" }
```

---

## Step 6 — Supabase production check

```
Post-deploy manual step — Supabase RLS:
1. Verify Row Level Security policies allow reads from the production domain
2. Confirm SUPABASE_SERVICE_ROLE_KEY is set in Vercel (needed for server-side mutations)
3. Confirm create_complete_order RPC exists in production DB:
   SELECT proname FROM pg_proc WHERE proname = 'create_complete_order';
   If missing: run migration from SITES/loja-jocril/supabase/migrations/20251227200000_create_complete_order_rpc.sql
```

---

## Step 7 — Final build verification

```bash
npm run build   # must pass clean before pushing to Vercel
```

If pushing via git:
```bash
git status      # confirm clean tree
git log --oneline -5   # confirm all B5-B7 commits visible
```

Then push to trigger Vercel deployment (Maria approves the push/deploy).

---

## Step 8 — Post-deploy smoke test

After Vercel deployment is live, test these paths:

```
GET  /                              → 200, homepage loads
GET  /produtos                      → 200, product list loads from Supabase
GET  /carrinho                      → 200, cart page loads
GET  /api/webhooks/eupago           → 200, {"status":"ok"}
POST /api/orders (empty)            → 400, Zod validation errors
GET  /admin (unauthenticated)       → redirect to /sign-in
GET  /conta (unauthenticated)       → redirect to /sign-in
```

---

## Step 9 — Handoff

Create: `AI_OS/SESSION-PROMPTS/SESSIONS/2026-04-19/2026-04-19_B8_handoff.md`

Include:
- Vercel env var checklist (all vars)
- Post-deploy manual steps (Eupago, Clerk, Supabase)
- Smoke test results
- Any issues found
- Project status: DONE or [list what remains]

Update `docs/DOCS_INDEX.md`.

---

## Notes on what NOT to do
- Do not configure custom build caching — Vercel handles it
- Do not add `output: 'standalone'` — not needed for Vercel
- Do not add analytics or monitoring in this session — future scope
- Do not modify the Supabase schema — read-only ops only in B8
